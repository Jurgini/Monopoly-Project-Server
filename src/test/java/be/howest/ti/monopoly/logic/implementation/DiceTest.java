package be.howest.ti.monopoly.logic.implementation;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.junit.jupiter.api.Test;

import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.*;

class DiceTest {
    // todo write tests
//    @Test
//    void randomNumbers()
//    {
//        Game g = new Game("testgame", 2);
//        Player p1 = new Player("Tuur", "testgame-Tuur");
//        Player p2 = new Player("Jur", "testgame-Jur");
//        g.addPlayer(p1);
//        g.addPlayer(p2);
//        p1.setCurrentTile(new MonopolyBoard().getTile(0));
//        p2.setCurrentTile(new MonopolyBoard().getTile(0));
//        g.buyProperty(p1, p1.getCurrentTileName());
//    }
}